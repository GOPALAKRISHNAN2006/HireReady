# Contributing

Thanks for your interest in contributing to HireReady. Please follow these guidelines:

1. Fork the repository and create a topic branch: `git checkout -b feature/my-feature`
2. Run tests and lint before submitting: `npm test` in `server` and `npm run lint` in `client`.
3. Open a pull request describing the change and its motivation.
